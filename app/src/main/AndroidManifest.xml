<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tugastts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DataDosenActivity"></activity>
        <activity android:name=".DaftarKRSActivity" />
        <activity android:name=".DataMahasiswaActivity" />
        <activity android:name=".LihatKelasActivity" />
        <activity android:name=".ListKRSActivity" />
        <activity android:name=".CreateKRSActivity" />
        <activity android:name=".CreateMatkulActivity" />
        <activity android:name=".ListMatkulActivity" />
        <activity android:name=".CreateMahasiswaActivity" />
        <activity android:name=".ListMahasiswaActivity" />
        <activity android:name=".CreateDosenActivity" />
        <activity android:name=".ListDosenActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".DosenActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>